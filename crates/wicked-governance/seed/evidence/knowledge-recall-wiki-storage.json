{
  "content": [
    {
      "text": "{\"items\":[{\"body_snippet\":\"## Decision\\n\\n### 1. Storage lives behind three traits (built)\\n- **`GraphRead`** \u2014 `capabilities`, `get_node`, `find_symbols`, `neighbors`, `traverse`, `stats`.\\n- **`GraphWrite`** \u2014 `begin_batch`, `commit_batch`, `upsert_nodes`, `upsert_edges`.\\n- **`GraphStore: GraphRead + GraphWrite + Send`** \u2014 auto-implemented; the common local case.\\n\\n**Why split read from write?** An external DB scales **many readers** (MCP handlers, agents,\\nrankers) independently of a **single writer** (the indexer). They want different connection types\\n(a pool / read replica vs. one writer) and different sharing guarantees. Rankers/tools take\\n`&dyn GraphRead`, so they structurally cannot mutate the graph. Trait upcasting (`&dyn GraphStore`\\n\u2192 `&dyn GraphRead`, stable Rust \u22651.86) lets the local single-object case still feed read-only APIs.\\n\\n### 2. One factory seam (built)\\n`wicked_estate_store::open_store(spec) -> Result<Box<dyn GraphStore>>` parses a connection spec\\n(`:memory:`, `sqlite://path`, bare path, `postgres://\u2026`, `surrealdb://\u2026`). Every entrypoint\\n(CLI, MCP, bench, indexer) opens its store here. Adding a backend = **one match arm + one module**;\\nzero caller changes. Postgres/SurrealDB currently return a clear `Error::Invalid(\\\"\u2026designed\u2026not built\\\")`.\\n\\n### 3. Capability negotiation (built)\\n`GraphRead::capabilities() -> StoreCapabilities { full_text_search, vector_search,\\nserver_side_traversal, transactional_batch, shared_writers }`. Retrieval (W5) **adapts**: if a\\nbackend lacks `vector_search`, RRF fusion falls back to client-side; if it lacks\\n`server_side_traversal`, traversal degrades to chunked `neighbors` calls. SQLite reports\\n`server_side_traversal=true, shared_writers=false`; a Postgres/server backend would report\\n`shared_writers=true`.\\n\\n### 4. Sync core now; async is a documented escape hatch (decision)\\nThe traits are **synchronous**. Justification: indexing is offline/batch, queries are **bounded\\nsingle-round-trip** (CTE / server-side `traverse`), and the MCP server's concurrency is modest.\\nA remote backend therefore uses a **connection pool + bulk batching + a blocking driver** (e.g.\\nsync `postgres`, or `block_on` against a shared runtime) behind the sync trait \u2014 the network cost\\nis round-trip *count*, which our design already minimizes, not async ceremony.\\n**If** a high-concurrency hosted server ever demands it, the migration is mechanical and contained:\\nmake `GraphRead`/`GraphWrite` `#[async_trait]`, add a runtime to the CLI/MCP entrypoints; callers\\nalready go through `open_store` + the trait, so the blast radius is the trait defs + impls, not the\\ndomain logic. We do **not** pay that cost speculatively (a Universal Don't: no over-engineering).\\n\\n### 5. Concurrency model (design)\\n`GraphStore: Send` (not `Sync`) \u2014 because a single-connection SQLite store is `!Sync`. Shared\\ndeployments wrap appropriately: the embedded store behind `Arc<Mutex<_>>` for a multi-handler MCP\\nserver; an external store as a **pool-backed handle that is itself `Sync`** (the pool is `Sync`),\\nhanding each reader its own connection. `shared_writers` advertises whether concurrent writers are\\nsafe. A `SharedGraphRead: GraphRead + Sync` marker can be added when the server backend lands.\\n\\n### 6. Network robustness (design \u2014 partially pre-paid)\\n- **Idempotency:** upserts already key on `Edge::dedup_key` / `Symbol` PK with higher-confidence-wins\\n  merge, so a retried batch is safe. This carries directly to `ON CONFLICT` over Postgres.\\n- **Batching:** `begin_batch`/`commit_batch` map to a\",\"class\":\"chunk\",\"label\":\"## Decision\\n\\n### 1. Storage lives behind three traits (built\",\"node_id\":\"kchunk synthetic 01a05050-ae98-7440-b655-168f6b79a821:\",\"score\":0.02459016393442623,\"source\":\"wicked-estate://docs/adr/ADR-003-storage-backends.md\"},{\"body_snippet\":\"PAT-1403 (error pattern rule): One writer per store file: writes go through a single-writer seam (the crew actor in-daemon, or exactly one CLI process offline); a daemon-held store is never CLI-written (DES-OUTGOV-008 \u2014 record the crew-api transport instead). \u2014 rationale source: storage-doctrine.md@33337819b8d43c15fffb320239f9a1013287d4cf#PAT-1403\",\"class\":\"chunk\",\"label\":\"PAT-1403 (error pattern rule): One writer per store file: wr\",\"node_id\":\"kchunk synthetic rule-rationale/PAT-1403:\",\"score\":0.024193548387096774,\"source\":\"storage-doctrine.md@33337819b8d43c15fffb320239f9a1013287d4cf#PAT-1403\"},{\"body_snippet\":\"POL-1401 (error policy rule): The embedded durable store (SQLite, ACID/WAL, crash-safe) is the zero-infra default; Postgres is optional behind a feature/profile \u2014 the local case never requires a server to run. \u2014 rationale source: storage-doctrine.md@33337819b8d43c15fffb320239f9a1013287d4cf#POL-1401\",\"class\":\"chunk\",\"label\":\"POL-1401 (error policy rule): The embedded durable store (SQ\",\"node_id\":\"kchunk synthetic rule-rationale/POL-1401:\",\"score\":0.023809523809523808,\"source\":\"storage-doctrine.md@33337819b8d43c15fffb320239f9a1013287d4cf#POL-1401\"},{\"body_snippet\":\"## \u00a7fan-out \u2014 one import, every lane a governed run reads\\n\\nEnforcement and discovery deliberately read DIFFERENT stores in the same governed run\\n(post-FINDING-067: workers' gates read the operational daemon store; the injected estate MCP\\nbinds the run repo's code-graph db). An import into one home silently misses the other lane, so\\nevery import is a fan-out keyed on stable `PAT-/POL-` ids (AW-5 / arch-R3):\\n\\n1. **Enforcement copy** \u2192 crew `POST /api/v1/governance/{policies,rules}` for live daemons\\n   (audited), or `wicked-core rules ingest` for offline stores \u2014 never the CLI against a\\n   daemon-held store (single-writer invariant).\\n2. **Discovery copy** \u2192 the repo/project graph as `NodeKind::Rule`.\\n3. **Rationale** \u2192 the knowledge sidecar, scope `wiki:<area>`, source = the wiki URI.\\n\\nEvery import ends with per-lane smoke verification (`rules/preview`, `rules.recall`,\\n`knowledge.recall`) against the same `--db` a worker is handed.\\n\\n**Cross-repo doctrine placement (arch-R20):** graphs and sidecars are per-repo, and edges do not\\nresolve across repos \u2014 but the highest-value rules (root CLAUDE.md, TARGET-ARCHITECTURE, event\\ngrammar) span repos. Default: **replicate-to-every-repo** \u2014 the fan-out manifest's\\n`scope: workspace` flag copies the rule into every live repo's discovery store (pure data,\\nzero engine change; id-keyed idempotent re-ingest keeps the N copies in sync). The alternative\\n(a workspace-root store with new resolution machinery) is deliberately deferred unless\\nreplication cost bites; AW-6 records the estate-owner ruling on this section.\",\"class\":\"chunk\",\"label\":\"## \u00a7fan-out \u2014 one import, every lane a governed run reads\\n\\nE\",\"node_id\":\"kchunk synthetic 01a05050-aedf-7b20-8628-9ccd1b5b8543:\",\"score\":0.0234375,\"source\":\"wicked-estate://docs/adr/ADR-011-architecture-wiki.md\"},{\"body_snippet\":\"## Infrastructure\\n\\n| Dir | What it does | Stack | Status |\\n|---|---|---|---|\\n| `wicked-bus` | Durable event fabric for agent ecosystems \u2014 at-least-once delivery with restart-durable retry, dead-lettering + operator replay, causality tracing, tiered storage, CAS, and a push-over-durable-poll daemon. Zero infra: the durability substrate is embedded SQLite (ACID/WAL), not a server to run. Single-host today (Unix-socket push, no remote transport). JS/ESM, v2.3.2. | JS | \ud83d\udd28 active |\\n| `wicked-brain` | **\ud83d\udce6 Retired (2026-08, Phase 5-S7)** \u2192 consolidated into `wicked-estate` (memory/knowledge/search; zero-loss migration), agent surface = wicked-garden's `mem`/`search` domains. npm package deprecated, repo archived. `~/.wicked-brain` stays on disk as a frozen archive \u2014 never delete it. | JS | \ud83d\udce6 retired |\\n| `wicked-core` | In-process composition runtime **and** the execution engine behind wicked-crew. Single-writer actor (no SQLite races) that also drives workflows-as-data: `WorkflowDef` JSON \u2192 data-driven planning \u2192 skills-driven CLI invocation \u2192 governed gates. See `wicked-core/README.md` + `.product/DES-EXEC-001`. | Rust | \ud83d\udd28 active |\\n| `wicked-ci` | Shared CI pipeline tooling. | \u2014 | infra |\\n| `wicked-installer` | Registry-driven interactive installer for the wicked-* family \u2014 dependency-aware, cross-CLI (Claude Code, Antigravity, Codex, OpenCode, Pi). The product sites market `npx wicked-installer` as the primary install path. npm v0.4.x. | TS | \ud83d\udd28 active |\\n| `wicked-web` | Shared site chrome (Base/Topbar/Footer/SameGarden) consumed by the product sites via git dependency \u2014 a library, no site of its own. (wicked.garden is retired; the family apex is wickedagile.com, per-product sites are w{g,c,e,s}.wickedagile.com; wt and wi now point at their successors.) | TS | infra |\\n| `wickedagile` | wickedagile.com. | TS | infra |\",\"class\":\"chunk\",\"label\":\"## Infrastructure\\n\\n| Dir | What it does | Stack | Status |\\n|\",\"node_id\":\"kchunk synthetic 01a05050-ae52-7643-8045-3b305a94018c:\",\"score\":0.023076923076923078,\"source\":\"workspace://CLAUDE.md\"},{\"body_snippet\":\"PAT-1402 (error pattern rule): Storage lives behind the `GraphRead`/`GraphWrite` traits and a single `open_store(spec)` factory \u2014 a new backend drops in as one factory arm, never as call-site branching. \u2014 rationale source: storage-doctrine.md@33337819b8d43c15fffb320239f9a1013287d4cf#PAT-1402\",\"class\":\"chunk\",\"label\":\"PAT-1402 (error pattern rule): Storage lives behind the `Gra\",\"node_id\":\"kchunk synthetic rule-rationale/PAT-1402:\",\"score\":0.022058823529411763,\"source\":\"storage-doctrine.md@33337819b8d43c15fffb320239f9a1013287d4cf#PAT-1402\"}]}",
      "type": "text"
    }
  ],
  "isError": false
}